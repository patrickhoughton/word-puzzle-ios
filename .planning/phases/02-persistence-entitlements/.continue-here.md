---
phase: 02-persistence-entitlements
task: 0
total_tasks: 3
status: not_started
last_updated: 2026-08-28T21:32:41.309Z
---

<current_state>
Phase 2 (Persistence & Entitlements) is 4/5 plans complete and fully merged to `main`. Only plan 02-05 remains, which will close out Phase 2 entirely. 02-05 has not been started — no tasks executed yet.
</current_state>

<completed_work>

- 02-01: App Store Connect setup (agreement Active, app record "Word Puzzle Unlimited", IAP product `com.patrickhoughton.wordpuzzle.unlimited` at $2.99) + `WordPuzzle.storekit` + target membership + scheme StoreKit Configuration wiring (written directly as XML after the Edit Scheme GUI silently failed to persist twice) — Done, verified with a full green test suite run.
- 02-02: `GameRecord` SwiftData model + `PersistenceStore` (container, record, puzzlesPlayedToday, lifetime stats) — Done.
- 02-03: `ScoreCalculator` (locked CONTEXT D-02 formula) + `PersistenceStore.currentStreak()` derived at read time — Done.
- 02-04: `EntitlementStore` (StoreKit 2, `Transaction.currentEntitlements` as sole source of truth for `isPremium`, `purchaseUnlimited()`, `restore()` via `AppStore.sync()`) — code complete and frozen. Test suite written (5 XCTest cases via `SKTestSession`); 2/5 pass, 3/5 fail locally due to an unresolved environment issue (see Blockers below) — not a code defect.
</completed_work>

<remaining_work>

- Task 1: Instantiate and inject both `PersistenceStore` and `EntitlementStore` in `WordPuzzleApp` — not started.
- Task 2: DEBUG-only entitlement harness in `ContentView` — not started.
- Task 3 (checkpoint:human-action): Manual sandbox purchase and restore test on a real device, per CONTEXT D-06 — not started. Requires Patrick to switch the Xcode scheme's Run destination StoreKit Configuration dropdown back to **None** first (the local `.storekit` config would otherwise silently override the real App Store sandbox).
</remaining_work>

<decisions_made>

- Scheme StoreKit Configuration is now written directly as XML into `WordPuzzle.xcodeproj/xcshareddata/xcschemes/WordPuzzle.xcscheme` rather than via Xcode's Edit Scheme GUI — the GUI edit silently failed to persist to disk twice in this project. If any future scheme setting needs changing, prefer editing this file directly and verify with a real `xcodebuild test` run rather than trusting "I clicked it."
- Built `scripts/appstoreconnect.py` — a JWT-authenticated App Store Connect API client. Credentials live outside the repo at `~/.appstoreconnect/` (config.json + private_keys/), reusable across future iOS projects. Read operations (GET /v1/apps, GET .../inAppPurchasesV2) tested and working. Create operations (new app/IAP records) deliberately NOT scripted yet — verify against Apple's current OpenAPI spec when actually needed rather than trusting a hardcoded payload.
</decisions_made>

<blockers>

- **3/5 EntitlementStoreTests fail locally** with `SKInternalErrorDomain Code=3` / `notEntitled` from `SKTestSession`'s off-device buy simulation, specific to the iPhone 17 Simulator on this Mac. Exhaustively diagnosed and ruled out: parallel-executor contention, wrong simulator, stale simulator state, sandboxed Bash, headless vs GUI simulator, orphaned `storekitd` processes, Developer Mode disabled (enabled it — no change), CLI vs Xcode's own test runner (both fail identically), and a full Mac reboot (no change). A run against Patrick's physical iPhone gets further (the purchase itself succeeds) then fails differently at `finishTransaction` — confirms this is Simulator/environment-specific, not an app code defect. **Not pursuing further local fixes** — plan 02-05 Task 3's real sandbox purchase test is a different, unaffected code path and will serve as the authoritative proof of MON-02/MON-03 instead. Full diagnosis: `.planning/phases/02-persistence-entitlements/02-04-SUMMARY.md` → Issues Encountered.
</blockers>

<context>
This was a full wave-based `/gsd:execute-phase 2` run: Wave 1 (02-01 + 02-02 in parallel), Wave 2 (02-03 + 02-04 in parallel), each via worktree-isolated executor subagents, merged back into `main` by the orchestrator (with a couple of STATE.md/ROADMAP.md merge conflicts resolved along the way — normal for parallel waves touching shared doc files). Wave 3 (02-05) has not started.

Along the way, two side threads happened that are worth remembering: (1) Patrick asked about automating the App Store Connect / Xcode manual steps — resulted in `scripts/appstoreconnect.py` plus a saved cross-session memory (`appstoreconnect-api-automation.md`) about the pattern for future projects; (2) a long, ultimately-inconclusive dive into the EntitlementStoreTests Simulator issue above, which is now fully documented and consciously deferred rather than left ambiguous.

Also worth remembering for 02-05: this project's Xcode version (26.6) uses `PBXFileSystemSynchronizedRootGroup` for the `WordPuzzle/WordPuzzle/` and `WordPuzzle/WordPuzzleTests/` folders — any new file written into those folders on disk is automatically a member of that folder's target, no manual "Add to target" step needed. Only a SECOND target membership (like 02-01's `WordPuzzle.storekit` → `WordPuzzleTests`) needs explicit wiring.
</context>

<next_action>
Run `/gsd:execute-phase 02` (or just resume normally) to spawn plan 02-05. Task 1 and Task 2 are autonomous and can run via a normal executor. Task 3 is a `checkpoint:human-action` — it will pause for Patrick to do the real sandbox purchase/restore on a device, including the reminder to switch the Run scheme's StoreKit Configuration back to **None** first.
</next_action>
