---
phase: 01-word-engine-puzzle-generation
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - WordPuzzleTests/PerformanceTests.swift
autonomous: false
requirements: [PUZZ-03]

must_haves:
  truths:
    - "Puzzle generation completes in under 500ms on a physical iPhone, as verified by `XCTAssertLessThan(elapsed, 0.500)` in `PerformanceTests.swift`."
    - "The performance test passes when run on a physical iPhone (not Simulator)"
  artifacts:
    - path: "WordPuzzleTests/PerformanceTests.swift"
      provides: "XCTest hard-assertion 500ms gate + measure{} baseline test for PUZZ-03"
      contains: "XCTAssertLessThan"
  key_links:
    - from: "PerformanceTests"
      to: "generatePuzzle(from:) + WordList"
      via: "@testable import WordPuzzle"
      pattern: "generatePuzzle"
---

<objective>
Add the single performance test that verifies **PUZZ-03's** "puzzle generation completes in under 500ms on a physical device" success criterion. This is the one test that MUST use XCTest (`measure {}` and `XCTAssertLessThan`) rather than Swift Testing, and the one verification that MUST run on a real iPhone rather than the Simulator (Simulator runs on Mac hardware and is not a valid benchmark — Pitfall 6).

Purpose: Close the last Phase 1 success criterion with an on-device benchmark.
Output: `WordPuzzleTests/PerformanceTests.swift` (XCTest), green on a physical device.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md
@.planning/phases/01-word-engine-puzzle-generation/01-02-SUMMARY.md

<notes>
- **STALE RESEARCH SIGNATURE WARNING (read before touching RESEARCH.md examples):** RESEARCH.md Pattern 2 and Pattern 4 (and the "XCTest Performance Test" code example) show an OUTDATED call `generatePuzzle(from wordSet: Set<String>)` / `generatePuzzle(from: wordList.words)`. That signature is WRONG for this codebase. The interface locked in Plan 02 (`01-02-PLAN.md` `<interfaces>` block) and delivered by `01-02-SUMMARY.md` is:
  `generatePuzzle(from wordList: WordList, maxAttempts: Int = 1000) throws -> Puzzle`
  You MUST call it as `try generatePuzzle(from: wordList)` where `wordList` is a `WordList` instance — NOT `wordList.words` and NOT a `Set<String>`. Copying the RESEARCH.md example verbatim will cause a compile failure. When in doubt, trust `01-02-SUMMARY.md` over RESEARCH.md.
- Patrick is a first-time iOS dev: this plan includes connecting an iPhone via USB, trusting the Mac, and selecting the device in the Xcode scheme.
- The performance test MUST be XCTest (`import XCTest`, `XCTestCase`, `measure {}`, `XCTAssertLessThan`). Swift Testing has NO performance equivalent (Pitfall 7). Keep it in its own file so it never mixes with the Swift Testing files from Plan 02.
- Use the hard-assertion approach (Option B in RESEARCH.md) as the primary gate: `let start = Date(); _ = try generatePuzzle(from: wordList); XCTAssertLessThan(Date().timeIntervalSince(start), 0.500)`. Also include the `measure {}` block for baseline/regression tracking.
- generatePuzzle signature comes from 01-02-SUMMARY.md — confirm it is `generatePuzzle(from: WordList, maxAttempts:)` (a `WordList` instance, not its `.words` Set).
- Do NOT include word-list load time in the measured window — load once in setUp, measure only generatePuzzle (the 500ms criterion is about generation, per ROADMAP success criterion 4).
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the XCTest performance test (PerformanceTests.swift)</name>
  <files>WordPuzzleTests/PerformanceTests.swift</files>
  <read_first>
    - .planning/phases/01-word-engine-puzzle-generation/01-02-SUMMARY.md (exact generatePuzzle + WordList signatures)
    - .planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md (Pattern 4 + Code Example: XCTest Performance Test; Pitfall 6, Pitfall 7 — NOTE: the code passes `wordList.words`/`Set<String>`, which is the STALE signature; the correct call is `generatePuzzle(from: wordList)` with a `WordList` instance per 01-02-SUMMARY.md)
  </read_first>
  <action>
Create `PerformanceTests.swift` in the WordPuzzleTests target (right-click WordPuzzleTests group -> New File -> Swift File -> name `PerformanceTests`; ensure it is added to the test target, NOT the app target). Implement EXACTLY (the generatePuzzle call below uses the CORRECT `WordList`-instance signature from Plan 02 — do NOT copy the `Set<String>` variant from RESEARCH.md; adjust only if 01-02-SUMMARY.md recorded a different signature):

```swift
import XCTest
@testable import WordPuzzle

final class PerformanceTests: XCTestCase {
    var wordList: WordList!

    override func setUp() async throws {
        wordList = WordList()
        await wordList.load()   // load once; NOT part of the measured window
    }

    // Primary PUZZ-03 gate: hard 500ms ceiling
    func testGenerationUnder500ms() throws {
        let start = Date()
        _ = try generatePuzzle(from: wordList)
        let elapsed = Date().timeIntervalSince(start)
        XCTAssertLessThan(elapsed, 0.500, "Puzzle generation took \(elapsed)s — exceeds 500ms target")
    }

    // Secondary: regression baseline (10 runs). Set baseline on device after first run.
    func testGenerationPerformanceBaseline() {
        measure {
            _ = try? generatePuzzle(from: wordList)
        }
    }
}
```

Build with Cmd+B to confirm it compiles. Do NOT rely on the Simulator result for sign-off — the Simulator will pass easily and is not the gate (Pitfall 6). The on-device run is Task 2.
  </action>
  <verify>
    <automated>grep -q "XCTAssertLessThan" WordPuzzleTests/PerformanceTests.swift && grep -q "import XCTest" WordPuzzleTests/PerformanceTests.swift && grep -q "0.500" WordPuzzleTests/PerformanceTests.swift && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `PerformanceTests.swift` contains `import XCTest`, `final class PerformanceTests: XCTestCase`, `measure {`, and `XCTAssertLessThan(elapsed, 0.500`
    - The generatePuzzle call passes a `WordList` instance (`generatePuzzle(from: wordList)`), NOT a `Set<String>` / `wordList.words` (the stale RESEARCH.md signature)
    - The file does NOT contain `import Testing` or `@Test` (must be pure XCTest — Pitfall 7)
    - Word list load happens in `setUp`, outside the measured/asserted window
    - Cmd+B reports Build Succeeded
  </acceptance_criteria>
  <done>PerformanceTests.swift compiles with a hard 500ms XCTAssertLessThan gate plus a measure{} baseline test.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Run the performance test on a physical iPhone</name>
  <what-built>The XCTest 500ms gate from Task 1. Success criterion #4 explicitly requires a physical-device run — this checkpoint walks Patrick through connecting an iPhone and running the test on it.</what-built>
  <read_first>
    - .planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md (Pitfall 6: Simulator vs device gap; Open Question 3: any iPhone 12+ clears 500ms; Pitfall 4: setting the measure baseline)
  </read_first>
  <how-to-verify>
    1. Connect your iPhone to the Mac with a USB/USB-C cable.
    2. On the iPhone, if prompted "Trust This Computer?", tap Trust and enter your passcode.
    3. In Xcode, at the top of the window next to the scheme name, click the destination dropdown (where it currently says a Simulator like "iPhone 16"). Under "iOS Device", select your connected iPhone by name.
       - If the device shows "Preparing" or needs Developer Mode: on the iPhone go to Settings -> Privacy & Security -> Developer Mode -> toggle ON, then restart the iPhone if prompted.
    4. Open `WordPuzzleTests/PerformanceTests.swift`. Click the diamond next to `func testGenerationUnder500ms()` to run just that test on the device. It MUST pass (green checkmark) — meaning generation completed under 500ms on real hardware.
    5. Then run `testGenerationPerformanceBaseline()` the same way. After it finishes, click the gray diamond in the test result -> "Set Baseline" (Pitfall 4 — without this the measure test never gates regressions). Record the reported average time.
    6. Run the full suite once on the device with Cmd+U to confirm all Plan 02 tests + this performance test are green together.
  </how-to-verify>
  <acceptance_criteria>
    - `testGenerationUnder500ms` passes (green) when run on a physical iPhone (device name visible, not a Simulator)
    - The reported elapsed time is under 0.500s
    - A baseline has been set for `testGenerationPerformanceBaseline` on the device
    - Full Cmd+U suite is green on the device
  </acceptance_criteria>
  <resume-signal>Type "passed on device: <elapsed>ms" with the measured time, or paste the failure if generation exceeded 500ms.</resume-signal>
</task>

</tasks>

<verification>
- `PerformanceTests.swift` is pure XCTest with a hard `XCTAssertLessThan(elapsed, 0.500)` gate
- The test passes on a physical iPhone (not Simulator)
- A measure{} baseline is set on the device
- All Phase 1 tests (Plan 02 + this) green together on device via Cmd+U
</verification>

<success_criteria>
- PUZZ-03 (500ms): puzzle generation verified under 500ms on real hardware — the final Phase 1 success criterion is closed
- Performance test correctly uses XCTest, isolated from the Swift Testing files
</success_criteria>

<output>
After completion, create `.planning/phases/01-word-engine-puzzle-generation/01-03-SUMMARY.md`. Record the physical device model used and the measured generation time in ms — this is the evidence for ROADMAP success criterion #4 and feeds `/gsd:verify-work`.
</output>
