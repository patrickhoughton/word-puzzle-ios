---
phase: 01-word-engine-puzzle-generation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - WordPuzzle.xcodeproj
  - WordPuzzle/WordPuzzleApp.swift
  - WordPuzzle/ContentView.swift
  - WordPuzzle/PuzzleEngine/
  - WordPuzzle/Resources/enable-clean.txt
  - scripts/filter_profanity.py
  - scripts/enable1.txt
  - scripts/ldnoobw-en.txt
autonomous: false
requirements: [PUZZ-02]
user_setup:
  - service: Xcode
    why: "Xcode 26 is not installed (only Command Line Tools present). Required for all Swift compilation, the iOS SDK, Simulator, and the test runner. ~10GB download from the Mac App Store."
    dashboard_config:
      - task: "Install Xcode 26 from the Mac App Store and accept the license"
        location: "Mac App Store -> search 'Xcode' -> Get"

must_haves:
  truths:
    - "Xcode 26 is installed and `xcodebuild -version` reports a version (no CommandLineTools error)"
    - "A WordPuzzle Xcode App project exists and builds with zero errors"
    - "The bundled enable-clean.txt contains 170,000+ words and none from the LDNOOBW English blocklist"
    - "enable-clean.txt is listed in the app target's Copy Bundle Resources build phase"
  artifacts:
    - path: "WordPuzzle.xcodeproj/project.pbxproj"
      provides: "Xcode App project with WordPuzzle app target and WordPuzzleTests test target"
      contains: "WordPuzzleTests"
    - path: "scripts/filter_profanity.py"
      provides: "One-time build script that removes LDNOOBW blocklist words from ENABLE"
      min_lines: 15
    - path: "WordPuzzle/Resources/enable-clean.txt"
      provides: "Profanity-filtered ENABLE word list bundled as an app resource"
      min_lines: 170000
    - path: "WordPuzzle/PuzzleEngine"
      provides: "Filesystem-backed group that will hold all engine code (created empty in this plan)"
  key_links:
    - from: "WordPuzzle app target"
      to: "WordPuzzle/Resources/enable-clean.txt"
      via: "Copy Bundle Resources build phase"
      pattern: "enable-clean.txt"
---

<objective>
Install Xcode 26, create the WordPuzzle iOS App project, scaffold the empty `PuzzleEngine/` group, and produce + bundle a profanity-filtered ENABLE word list. This is Wave 0 — nothing else in the phase can proceed until Xcode exists and the project compiles.

This plan addresses **PUZZ-02** (word list filtered for profanity before puzzle generation) by shipping a cleaned `enable-clean.txt` produced by a one-time Python script.

Purpose: Establish the buildable foundation and the clean dictionary contract that every subsequent phase depends on.
Output: A compiling WordPuzzle.xcodeproj, an empty PuzzleEngine group, and a bundled enable-clean.txt in Copy Bundle Resources.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-word-engine-puzzle-generation/01-CONTEXT.md
@.planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md
@.planning/REQUIREMENTS.md

<notes>
- Patrick is a FIRST-TIME iOS/Xcode developer. Every Xcode UI step must be spelled out (which menu, which button, which checkbox). Do not assume familiarity.
- Xcode is confirmed NOT installed: `xcode-select -p` returns `/Library/Developer/CommandLineTools`, and `xcodebuild -version` errors with "requires Xcode".
- ENABLE mirror: https://raw.githubusercontent.com/dolph/dictionary/master/enable1.txt (172,823 lowercase words, one per line).
- LDNOOBW English list: https://raw.githubusercontent.com/LDNOOBW/List-of-Dirty-Naughty-Obscene-and-Otherwise-Bad-Words/master/en (CC-BY 4.0).
- Project name is WordPuzzle. Use SwiftUI App template, iOS 17+ deployment target, Swift Testing test target ("Include Tests" checked at creation).
</notes>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Install Xcode 26 (human-only Mac App Store step)</name>
  <what-built>Nothing yet — this checkpoint installs the prerequisite. Xcode cannot be installed via CLI (it is a Mac App Store product requiring an Apple ID sign-in and a ~10GB GUI download).</what-built>
  <read_first>
    - .planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md (Pitfall 2: Xcode Not Installed; Environment Availability table)
  </read_first>
  <how-to-verify>
    1. Open the Mac App Store (Apple menu -> App Store, or Spotlight -> "App Store").
    2. Search "Xcode". Click "Get" then "Install" on the Xcode entry (free, ~10GB, allow 30-60 min).
    3. When installation finishes, open Xcode once. Accept the license agreement when prompted. Let it install any additional required components.
    4. Set Xcode as the active developer directory: open Terminal and run `sudo xcode-select -s /Applications/Xcode.app/Contents/Developer` (enter your Mac password).
    5. Verify in Terminal: run `xcodebuild -version`. It MUST print something like `Xcode 26.x` and NOT the "requires Xcode, but active developer directory ... is a command line tools instance" error.
  </how-to-verify>
  <acceptance_criteria>
    - `xcodebuild -version` prints a line starting with `Xcode ` and a version number 26 or higher
    - `xcode-select -p` prints `/Applications/Xcode.app/Contents/Developer` (NOT `/Library/Developer/CommandLineTools`)
  </acceptance_criteria>
  <resume-signal>Type "installed" once `xcodebuild -version` prints Xcode 26.x, or describe the error you hit.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Generate the profanity-filtered ENABLE word list</name>
  <files>scripts/filter_profanity.py, scripts/enable1.txt, scripts/ldnoobw-en.txt, WordPuzzle/Resources/enable-clean.txt</files>
  <read_first>
    - .planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md (Pattern 3: Profanity Filter Script; Pitfall 1: ENABLE format; Open Question 1: word count after filter)
  </read_first>
  <action>
Create the `scripts/` directory at the repo root and download the two source lists, then write and run the filter script. Do all of this before the Xcode project exists (the cleaned file is added to the bundle in Task 4).

1. Create directory: `mkdir -p /Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/scripts`
2. Download ENABLE:
   `curl -fsSL https://raw.githubusercontent.com/dolph/dictionary/master/enable1.txt -o /Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/scripts/enable1.txt`
3. Download the LDNOOBW English blocklist:
   `curl -fsSL https://raw.githubusercontent.com/LDNOOBW/List-of-Dirty-Naughty-Obscene-and-Otherwise-Bad-Words/master/en -o /Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/scripts/ldnoobw-en.txt`
4. Write `scripts/filter_profanity.py` with EXACTLY this behavior (a whole-word case-insensitive set-difference; do NOT do substring matching, which would over-remove words like "assess"):

```python
#!/usr/bin/env python3
# filter_profanity.py
# One-time build tool. Run once, commit the output, never runs at app runtime.
# Usage: python3 filter_profanity.py enable1.txt ldnoobw-en.txt enable-clean.txt
import sys

def main(enable_path, blocklist_path, output_path):
    with open(blocklist_path, encoding="utf-8") as f:
        blocklist = {line.strip().lower() for line in f if line.strip()}
    with open(enable_path, encoding="utf-8") as f:
        words = [line.strip().lower() for line in f if line.strip()]
    cleaned = [w for w in words if w not in blocklist]
    print(f"Original: {len(words)} words")
    print(f"Removed:  {len(words) - len(cleaned)} words")
    print(f"Result:   {len(cleaned)} words")
    with open(output_path, "w", encoding="utf-8") as f:
        f.write("\n".join(cleaned) + "\n")

if __name__ == "__main__":
    main(sys.argv[1], sys.argv[2], sys.argv[3])
```

5. Create the Resources directory and run the script writing output straight into it:
   `mkdir -p /Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/WordPuzzle/Resources`
   `cd /Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/scripts && python3 filter_profanity.py enable1.txt ldnoobw-en.txt ../WordPuzzle/Resources/enable-clean.txt`
6. Confirm the printed "Result" count is greater than 170,000 (Open Question 1 expects ~172,000 after filtering). If the result is below 170,000, STOP — the blocklist download likely failed or matched too aggressively; investigate before proceeding.

Note: `WordPuzzle/Resources/` may be created before the Xcode project (Task 3) exists — that is fine. Task 3 creates the project at the same repo root, and Task 4 adds this file into the project.
  </action>
  <verify>
    <automated>test $(wc -l < /Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/WordPuzzle/Resources/enable-clean.txt) -gt 170000 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/filter_profanity.py` exists and contains `w not in blocklist`
    - `WordPuzzle/Resources/enable-clean.txt` exists with more than 170000 lines (`wc -l` > 170000)
    - The word "fart" (harmless, in ENABLE, not in LDNOOBW) is still present: `grep -qx fart WordPuzzle/Resources/enable-clean.txt` exits 0
    - A representative LDNOOBW word is absent: after loading, `grep -ixq "$(head -1 scripts/ldnoobw-en.txt)" WordPuzzle/Resources/enable-clean.txt` exits NON-zero (blocklist word removed). If the first blocklist entry is a phrase with spaces, pick a single-token entry to test.
  </acceptance_criteria>
  <done>enable-clean.txt exists in WordPuzzle/Resources/ with >170,000 words; the script and its two input files are saved under scripts/.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Create the WordPuzzle Xcode App project</name>
  <what-built>The WordPuzzle App project must be created through Xcode's New Project wizard (Xcode does not offer a scriptable project generator for a first-time-friendly SwiftUI + test-target setup). This checkpoint walks Patrick through the GUI and verifies the result builds.</what-built>
  <read_first>
    - .planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md (Architecture Patterns: Recommended Project Structure; State of the Art: automatic signing)
    - .planning/phases/01-word-engine-puzzle-generation/01-CONTEXT.md (D-01: full Xcode App project, engine in PuzzleEngine/ group, tests in existing test target)
  </read_first>
  <how-to-verify>
    Follow these EXACT steps in Xcode:
    1. Open Xcode. On the welcome window click "Create New Project..." (or File -> New -> Project).
    2. Platform tab: select "iOS". Template: select "App". Click Next.
    3. Fill the options:
       - Product Name: `WordPuzzle`
       - Team: select your Apple ID / Personal Team (add your Apple ID under Xcode -> Settings -> Accounts if none appears).
       - Organization Identifier: `com.patrickhoughton` (or your reverse-domain).
       - Interface: `SwiftUI`
       - Language: `Swift`
       - Storage: `None`
       - CHECK "Include Tests" (this creates the WordPuzzleTests target — required by D-01).
       Click Next.
    4. Save location: choose `/Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/`. UNCHECK "Create Git repository on my Mac" (the repo already exists). Click Create.
       - The project must land so that `WordPuzzle.xcodeproj` sits at `/Users/patrickhoughton/Documents/GitHub/word-puzzle-ios/WordPuzzle.xcodeproj`. If Xcode nests it in an extra `WordPuzzle/` folder, that is acceptable as long as the executor records the actual path in the SUMMARY.
    5. Set the deployment target: select the project (top of the Navigator) -> WordPuzzle target -> General tab -> Minimum Deployments -> set iOS to `17.0`.
    6. Confirm signing: target -> Signing & Capabilities -> ensure "Automatically manage signing" is checked and a Team is selected.
    7. Build: press Cmd+B. It MUST succeed with "Build Succeeded" and zero errors.
  </how-to-verify>
  <acceptance_criteria>
    - `WordPuzzle.xcodeproj/project.pbxproj` exists somewhere under the repo root and contains the string `WordPuzzleTests`
    - The project's iOS deployment target is 17.0: `project.pbxproj` contains `IPHONEOS_DEPLOYMENT_TARGET = 17.0`
    - Cmd+B reports "Build Succeeded"
  </acceptance_criteria>
  <resume-signal>Type "built" once Cmd+B shows Build Succeeded, or paste any build error.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Add PuzzleEngine group and bundle enable-clean.txt</name>
  <what-built>An empty `PuzzleEngine/` filesystem-backed group (to hold engine code in the next plan) and the cleaned word list added to the app bundle. Both are GUI operations in Xcode's Navigator; this checkpoint verifies the resource ships with the app.</what-built>
  <read_first>
    - .planning/phases/01-word-engine-puzzle-generation/01-RESEARCH.md (Code Examples: Adding a File to Xcode App Bundle; Anti-Patterns: use "New Group with Folder", not virtual groups)
  </read_first>
  <how-to-verify>
    1. Create the PuzzleEngine group as a real folder:
       - In the Project Navigator, right-click the yellow `WordPuzzle` group (the app source group, NOT the project root or the Tests group).
       - Choose "New Group with Folder" (Xcode 16+). Name it `PuzzleEngine`. This creates a blue filesystem folder synced with the Navigator. Leave it empty for now.
    2. Add the word list resource:
       - Right-click the `WordPuzzle` group -> "Add Files to \"WordPuzzle\"...".
       - Navigate to `WordPuzzle/Resources/enable-clean.txt` (created in Task 2).
       - CHECK "Copy items if needed".
       - Under "Add to targets", CHECK the `WordPuzzle` app target (do NOT check the test target).
       - Click Add.
    3. Verify the resource is bundled: select the project -> WordPuzzle target -> Build Phases -> expand "Copy Bundle Resources". `enable-clean.txt` MUST appear in that list.
    4. Build again with Cmd+B to confirm still "Build Succeeded".
  </how-to-verify>
  <acceptance_criteria>
    - A `PuzzleEngine` folder exists on disk under the WordPuzzle app source directory
    - `WordPuzzle.xcodeproj/project.pbxproj` references `enable-clean.txt`
    - `enable-clean.txt` appears in Build Phases -> Copy Bundle Resources for the app target
    - Cmd+B reports "Build Succeeded"
  </acceptance_criteria>
  <resume-signal>Type "bundled" once enable-clean.txt is in Copy Bundle Resources and the build succeeds.</resume-signal>
</task>

</tasks>

<verification>
- `xcodebuild -version` reports Xcode 26.x (not the CommandLineTools error)
- WordPuzzle.xcodeproj builds with Cmd+B (Build Succeeded, zero errors)
- `WordPuzzle/Resources/enable-clean.txt` has >170,000 words and is in Copy Bundle Resources
- Empty `PuzzleEngine/` group exists as a filesystem folder
- `scripts/filter_profanity.py` reproduces the cleaned list from the two source files
</verification>

<success_criteria>
- Xcode 26 installed and active (`xcode-select -p` -> Xcode.app path)
- WordPuzzle App project + WordPuzzleTests target created, iOS 17 target, builds clean
- Profanity-filtered word list bundled and shipping with the app (PUZZ-02 groundwork)
- PuzzleEngine group ready to receive engine code in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/01-word-engine-puzzle-generation/01-01-SUMMARY.md`. Record the ACTUAL on-disk path of `WordPuzzle.xcodeproj` and the app source directory (in case Xcode nested it), plus the final word count printed by the filter script — Plan 02 needs the exact resource name and bundle location.
</output>
